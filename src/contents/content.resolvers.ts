import client from "../client";

export default {
  Content: {
    // user: ({ userId }) => {
    //   return client.user.findUnique({
    //     where: { id: userId },
    //   });
    // },
    // photos: ({ photos, id }) => {
    //   console.log(photos,id);
    // },
  },
};